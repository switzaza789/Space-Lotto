# 🚀 Space Lotto - Deployment & Verification Guide

คู่มือนี้จะพาคุณเปลี่ยนจากโหมดจำลอง (Mock) ไปสู่ระบบจริง (Real Blockchain) บน BSC Testnet อย่างปลอดภัยและมั่นใจ

---

## ✅ Phase 1: เตรียมความพร้อม (Pre-Flight Check)

1.  **Metamask Wallet:**
    *   ต้องมี Wallet ที่ติดตั้งบน Browser
    *   **เปลี่ยน Network** ไปที่ `BSC Testnet` (Chain ID: 97)
    *   **มีเหรียญ TBNB** (Testnet BNB) สำหรับจ่ายค่า Gas [ขอฟรีได้ที่ BNB Faucet](https://testnet.bnbchain.org/faucet-smart)

2.  **เหรียญ USDT (Testnet):**
    *   คุณต้องมี Address ของ USDT จำลองบน Testnet (ถ้าไม่มี ให้ Deploy Contract ERC20 ง่ายๆ ขึ้นมาเอง ตั้งชื่อว่า USDT ก็ได้)

---

## 🚀 Phase 2: ขึ้นระบบ Smart Contract (Launch)

1.  ไปที่ [Remix IDE](https://remix.ethereum.org/)
2.  สร้างไฟล์ `SpaceLotto.sol` และวาง Code ลงไป
3.  Compile (ใช้ Version 0.8.19 ขึ้นไป)
4.  **Deploy:**
    *   เลือก Environment: `Injected Provider - MetaMask`
    *   ใส่ Constructor Argument: `_usdtToken` (ที่อยู่ USDT ของคุณ)
    *   กด **Transact** และยืนยันใน MetaMask
5.  **🎉 สำคัญ:** ก๊อปปี้ Address ของ Contract ที่ได้ (เช่น `0xABC...123`) เก็บไว้!

---

## 🔗 Phase 3: เชื่อมต่อ Frontend (Link Up)

แก้ไขไฟล์ `src/App.js`:

1.  ค้นหา: `const USE_MOCK = true;`
    *   เปลี่ยนเป็น: `const USE_MOCK = false;` 🔴 (สำคัญมาก)
2.  ค้นหา: `const CONTRACT_ADDRESS = "..."`
    *   เปลี่ยนเป็น: Address ที่คุณเพิ่ง Deploy มา
3.  ค้นหา: `const USDT_ADDRESS = "..."`
    *   เปลี่ยนเป็น: Address ของ USDT Testnet ที่คุณใช้

---

## 🧪 Phase 4: การทดสอบจริง (Battle Test)

เมื่อรัน `npm start` คุณจะเห็นป้าย **🟢 LIVE CHAIN** มุมขวาล่าง ให้ทำตามนี้:

### 1. Connect & Approve
*   [ ] กด Connect Wallet -> ต้องเห็นเลขกระเป๋าจริง
*   [ ] กด **Approve USDT** -> MetaMask ต้องเด้งขออนุญาต -> กดยืนยัน -> รอจนกว่าปุ่มจะเปลี่ยนสถานะ

### 2. Buy Ticket (ซื้อตั๋ว)
*   [ ] กรอกเลขที่ชอบ (เช่น `7777`)
*   [ ] กด **LAUNCH TICKET**
*   [ ] MetaMask เด้ง -> กดยืนยัน (เสีย Gas จริงนิดหน่อย)
*   [ ] **รอ Block Confirm:** รอสักพัก (3-5 วินาที)
*   [ ] **Result:** ตั๋วต้องโผล่มาใน "My Tickets" ข้างขวา

### 3. Verify on Blockchain (เช็คความชัวร์)
*   [ ] นำ Transaction Hash ไปค้นใน [BscScan Testnet](https://testnet.bscscan.com/)
*   [ ] ต้องเห็นสถานะ **Success**

### 4. Sharing & Links (การแชร์)
*   [ ] คลิกดูตั๋วที่ซื้อแล้ว -> กดปุ่ม **Copy Link**
*   [ ] ตรวจสอบว่ามี Pop-up แจ้งเตือน **"📋 Link Copied!"** โผล่มาด้านบนสุด
*   [ ] ลองวางลิงก์ใน Browser Tab ใหม่ -> ระบบต้องเปิด Modal โชว์ตั๋วหมายเลขนั้นทันที
*   [ ] ต้องเห็นป้าย **👀 Shared** สีม่วงบนตั๋วเมื่อเปิดจากหน้าลิงก์

### 5. Performance & Accessibility (ประสิทธิภาพและการเข้าถึง)
*   [ ] **Tab Navigation:** ลองกดปุ่ม `Tab` บนคีย์บอร์ด -> ต้องเห็น Focus Ring ล้อมรอบปุ่มและ Input ทุกตัวชัดเจน
*   [ ] **Hydration Check:** รีเฟรชหน้าจอ -> ต้องไม่มี Error ใน Console เกี่ยวกับ "Hydration" หรือ "Math.random"
*   [ ] **Responsiveness:** ลองย่อหน้าจอเป็นมือถือ -> UI ต้องจัดวางสวยงามและปุ่มกดง่าย

### 6. Claim Prize (รับรางวัล)
*   *(ขั้นตอนนี้ยากหน่อยใน Testnet เพราะต้องรอ Chainlink VRF ขานเลข)*
*   แต่ถ้าคุณเป็น Owner ของ Contract คุณสามารถทดสอบฟังก์ชัน `withdraw` หรือฟังก์ชันอื่นๆ ได้
*   ถ้าถูกรางวัลจริง ปุ่ม **Check & Claim** จะต้องทำงานดึงเงินเข้ากระเป๋าคุณได้จริง

---

## 🚩 Troubleshooting (ปัญหาที่พบบ่อย)

*   **Error: "Execution Reverted"**: มักเกิดจากไม่ได้ Approve USDT หรือเงิน USDT ไม่พอ
*   **Error: "Internal JSON-RPC error"**: Gas Metamask ตั้งไว้ต่ำไป หรือ Contract มี Bug
*   **ตั๋วไม่ขึ้น:** ลอง Refresh หน้าจอ ถ้าของจริง ข้อมูลต้องไม่หาย!

ขอให้โชคดีกับการ Deploy ครับ! 🚀🌕
